#include <iostream>
#include <vector>
using namespace std;

int maxProfit(vector <int> &v){

    int n = v.size();
    int mini = v[0];
    int maxP = 0;

    for(int i =0 ; i < n ; i++){
        int cost = prices[i] - mini;
        maxP = 
    }
}