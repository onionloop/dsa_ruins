#include <iostream>
#include <climits>
#include <vector>
#include <algorithm>
using namespace std;


int maxSubArray(vector <int> ans){

    int maxN = INT_MIN;
    int sum = 0;
    for(int i = 0 ; i < ans.size() ; i++){
            
    }
}